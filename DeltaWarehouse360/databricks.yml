# This is a Databricks asset bundle definition for DeltaWarehouse360.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: DeltaWarehouse360
  uuid: 1cf3f77d-ac73-42fe-a0bc-63315fde426b
  version: 0.1.0

include:
  - resources/*.yml
  - resources/*/*.yml

artifacts:
  python_artifact:
    type: whl
    build: uv build --wheel

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  prod:
    mode: production
    workspace:
      host: https://dbc-0713ba39-d291.cloud.databricks.com
      root_path: /Workspace/Users/suganya_am@outlook.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: workspace
      schema: prod
    permissions:
      - user_name: suganya_am@outlook.com
        level: CAN_MANAGE
